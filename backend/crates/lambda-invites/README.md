# lambda-invites

`POST /admin/invites`, `POST /invites/redeem`, `POST /admin/invites/{code}/revoke`, `GET /admin/groups/{g}/invites`, plus the Cognito `PreSignUp` trigger. See [`../../../plans/05-auth-flow.md`](../../../plans/05-auth-flow.md).
