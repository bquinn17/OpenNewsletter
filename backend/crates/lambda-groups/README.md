# lambda-groups

`GET /me`, `PATCH /me`, `GET /config`, `GET /groups`, `GET /groups/{g}`, `PATCH /groups/{g}`, and member-management routes. See [`../../../plans/03-api-contract.md`](../../../plans/03-api-contract.md).
