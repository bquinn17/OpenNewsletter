# shared

Cross-cutting artifacts consumed by both frontend and backend. `openapi.yaml` is the API contract; TypeScript types are generated from it and Rust types are hand-written but contract-tested against it.
